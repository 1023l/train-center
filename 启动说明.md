# 超算中心（标注/训练平台）启动说明

## 一、启动服务

在 `c:\projects\train-center` 目录下执行：

```
C:\Users\Administrator\miniconda3\envs\yolo-bench\python.exe -m uvicorn server.main:app --host 0.0.0.0 --port 8000
```

> 使用 **yolo-bench** 环境（已装 fastapi/uvicorn 等依赖）。

## 二、访问

| 入口 | 地址 |
|---|---|
| 标注/前端页面 | http://127.0.0.1:8000/ |
| API 文档 | http://127.0.0.1:8000/docs |
| 健康检查 | http://127.0.0.1:8000/health |

## 三、验证是否启动成功

浏览器打开 `http://127.0.0.1:8000/health`，返回 `{"status": "ok"}` 即正常。

## 四、停止服务

```cmd
:: 找到占用 8000 端口的进程 PID
netstat -ano | findstr :8000

:: 结束该进程（把 <PID> 换成上面查到的数字）
taskkill /F /PID <PID>
```

## 五、目录说明

```
c:\projects\train-center\
├── server\        # FastAPI 后端（routes/services）
├── static\        # Vue 前端（5 个页面：数据管理/OCR标注/训练/模型管理/日志）
├── core\          # 核心逻辑（数据、导出、训练等）
├── data\          # 训练数据
│   ├── det_fabric\   # fabric 检测数据（images+labels，train/val 划分）
│   ├── det_text\     # 文字区检测数据
│   └── rec_text\     # OCR 识别数据
├── models\        # 训练产出的模型
│   ├── det\       # yolo 检测模型（fabric/text）
│   └── ocr\       # paddleocr rec 模型
└── PaddleOCR\     # PaddleOCR 源码（rec 训练）
```

## 六、注意事项

- **训练不会删除数据**：训练只是读取 `data/` 下的划分结果，原数据保留，可随时补充标注再训练。
- 标注数据流程：上传 → 去重 → Web 标注 → 导出 → 训练。
- 训练顺序固定：`det_fabric → det_text → rec_text`（串行，不可并行）。
- rec 训练需要 GPU（PaddleOCR 配置 `Global.use_gpu=true`）。

## 七、生产部署（可选）

项目根目录另有 Docker 方案（`deploy/` 及 `Dockerfile`），现场可容器化部署。
